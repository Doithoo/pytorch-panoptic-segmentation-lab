# 贡献指南

[English](CONTRIBUTING.md)

贡献应该帮助读者运行代码、理解一个决定或复现一次结果。行为改动保持聚焦，并用具体例子说明。

1. 使用聚焦分支，行为变化必须增加测试。
2. 提交 PR 前运行 `make check`，其中包含覆盖率报告。
3. 不提交数据集、大 checkpoint、凭据、Kaggle token 或生成产物。
4. 命令、字段、输出、指标或文件格式变化时，同步更新英文和简体中文页面。
5. 教程解释原理，指南给出步骤，参考资料定义准确规则，运行记录保存实测输出。

## 文档写法

优先告诉读者“做什么”和“看什么”：

- 写“运行 `inspect-data`，它会检查尺寸和 instance ID”，不要写“项目提供了可靠的预检能力”；
- 写出实际的 split、设备、seed 和指标，不要只写“这是一次可复现的实验”；
- 说明限制会造成什么后果，不要反复强调“不是官方结果”；
- 在代码名周围使用自然中文；`schema`、`thing`、`stuff`、`target` 和 CLI 名称如果是实际标识符就保持原样；
- 中英文页面需要描述相同的行为，但不必逐句对应。

避免使用“完整契约”“明确扩展点”“可审计流程”“教学证据”“不是隐藏能力”等自我评价或空泛标签，除非紧接着给出具体文件、命令或测量结果。

修改指标时加入可手算的 void、thing、stuff、缺席类别和多图聚合案例。修改模型时加入无需下载权重的 CPU shape/backward 测试。数据转换器需要包含错误标签 fixture、划分证据、raw-to-train ID 表、crowd/void 行为和许可说明。

记录一次结果时，必须写出最终配置、源码 revision、数据和划分规则、checkpoint 哈希、环境、选择规则、耗时和限制。不得把占位或估算指标写成完成结果。

checkpoint 加载、模型重建、压缩包解压、子进程、依赖安装、Kaggle runner 和路径解析都属于安全敏感变更。PR 中说明相关输入和信任假设，变化时同步更新 `SECURITY.md`。
