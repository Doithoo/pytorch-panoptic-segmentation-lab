# 参与贡献

贡献应保持学习路径清晰，并保证实验可复现。

1. 使用职责单一的分支，并为行为变化添加测试。
2. 提交 PR 前运行 `make check`。
3. 不要提交数据集、checkpoint、凭据或生成的 artifacts。
4. 新增 target 字段、模型输出、指标或配置项时同步更新文档。

修改指标时请加入可手工核对的小样例；修改模型时请加入无需下载预训练权重的 CPU 输出尺寸测试。
